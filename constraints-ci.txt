# Direct CI quality-tool pins. This is not a full runtime/transitive lockfile;
# runtime dependencies and build dependencies remain resolved from pyproject.toml.
hypothesis==6.168.0
mypy==2.3.1
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.7
